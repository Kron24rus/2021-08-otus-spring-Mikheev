spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: data-test.sql
    schema: schema.sql
  shell:
    interactive:
      enabled: false

  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop

    #show-sql: true
