spring:
  data:
    mongodb:
      database: library

  shell:
    interactive:
      enabled: false

mongock:
  runner-type: "ApplicationRunner"
  change-logs-scan-package: com.mikheev.homework.mongocktest
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
  enabled: false
