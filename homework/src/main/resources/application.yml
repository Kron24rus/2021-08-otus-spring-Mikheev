spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      data-locations: data.sql
      schema-locations: schema.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
